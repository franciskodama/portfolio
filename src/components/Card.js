import React from 'react'


function Card({ title, tech, category, url, image }) {

  return (
    <div className='container'>
        <div>TESTE</div>
        {/* <img src={image} alt='main image project' className='card-image' />
        <p className='card-category'>{category}</p>
        <h2 className='card-title'>{title}</h2>
        <h2 className='card-tech'>{tech}</h2>
        <a href={url} className='card-explore'>explore project</a>
        <img src='' alt='icon explore project' className='card-icon' /> */}
    </div>
  )
}

export default Card
